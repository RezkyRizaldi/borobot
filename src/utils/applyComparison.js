/**
 *
 * @param {Number} firstValue
 * @param {Number} secondValue
 * @returns {Number} The comparison between two numbers.
 */
module.exports = (firstValue, secondValue) =>
  Math.abs(firstValue - secondValue);
